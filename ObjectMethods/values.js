const person = {
    firstName : "Anas",
    lastName :"MUhammed",
    age : 18
}

const values = Object.values(person)
console.log(values)


// Values means the values we provided for the keys in the object 
// by using the Object.values() method we can retrieve the values in an object as an array